import styled from "styled-components";

export const UsersTable = styled.table`
	width: 100%;
	text-align: left;
	border-collapse: separate;
	border-spacing: 0;
`;

export const TableHeadSort = styled.button`
	all: inherit;
	cursor: pointer;
	display: flex;
	flex-direction: row;
	align-items: center;
	gap: 0.5rem;
`;
